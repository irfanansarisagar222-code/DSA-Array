class Solution {
public:
    void moveZeroes(vector<int>& nums) {//O(n) Time Complexity
        int n=nums.size();
        int ans=0;

        for(int i=0;i<n;i++){
            if(nums[i]!=0){
                swap(nums[ans],nums[i]);
                ans++;
            }
        }
    }
};
